# --- core web ---
Flask>=3.0,<4.0
Flask-Login>=0.6
Flask-WTF>=1.2
flask-cors>=4.0
Flask-Limiter>=3.7

# --- db & util ---
pymongo>=4.6
python-dotenv>=1.0
email-validator>=2.1

# --- immagini ---
Pillow>=10.2
pillow-heif>=0.16 ; platform_system == "Windows"

# --- ML (versioni flessibili per Python/OS recenti) ---
torch>=2.6,<2.9
torchvision>=0.21,<0.24
